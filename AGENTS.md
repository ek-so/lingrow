# Agent instructions

## Git workflow

- Do **not** open pull requests.
- Commit on a working branch if needed, then **merge directly into `main`** and push `main`.
- Do not leave work sitting only on a PR branch.
